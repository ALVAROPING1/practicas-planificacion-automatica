These JSON contain the initial state and the goal of the problems so the
problem file can be generated with Python. As there are no fluents in
fast-downward it is hard to represent relations between numbers.
